# Notificaciones push

## Dependencias Requeridas

Instala estas dependencias antes de ejecutar el proyecto:

##### Dependencias principales
```bash
npm install @react-native-async-storage/async-storage date-fns
```

#### Dependencias de Expo (usar npx expo install)
```bash
npx expo install expo-notifications expo-task-manager expo-background-fetch
```